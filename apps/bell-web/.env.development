# Local dev default: points at the Firebase emulator (see `functions/package.json`'s
# `test:emulator` script for the matching --project/region). Override with a
# gitignored `.env.local` if your local emulator uses a different project id.
VITE_NOTIFY_FUNCTION_URL=http://127.0.0.1:5001/demo-game-master-bell/us-central1/call
