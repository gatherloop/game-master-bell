# Base URL of the deployed notify Cloud Function (see FR-W4).
# Copy this file to `.env.local` (gitignored) to override the committed
# `.env.development` default, or set it as a build-time env var in CI for
# production deploys.
VITE_NOTIFY_FUNCTION_URL=http://127.0.0.1:5001/demo-game-master-bell/us-central1/call
