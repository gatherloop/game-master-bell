node_modules/
dist/
build/
coverage/
/qr-codes/
*.tsbuildinfo
.DS_Store
*.log
.env
.env.local
.vitepress/cache/

# Firebase service-account JSON (FCM sending credentials) — never commit.
*fcm-service-account*.json

# pnpm deploy output (apps/api build artifact packaged for the VPS)
/api-deploy/
/api-deploy.tar.gz
